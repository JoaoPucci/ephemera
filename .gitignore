# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environment
venv/
.venv/
env/

# Local secrets & data
.env
*.db
*.db-journal
*.db-wal
*.db-shm

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Private runbook (local only)
DEPLOYMENT.md

# Security audit materials — kept local. The raw report lists still-open
# findings with severity; the per-finding response doc is handed to the
# auditor directly rather than published. Both live under audit/.
audit/

# Logs
*.log

# Coverage
.coverage
.coverage.*
htmlcov/

# Node (Vitest + Playwright)
node_modules/
coverage/
playwright-report/
test-results/
/playwright/.cache/
