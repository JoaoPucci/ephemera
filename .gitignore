# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environment
venv/
.venv/
env/

# Local secrets & data
.env
*.db
*.db-journal
*.db-wal
*.db-shm

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Private runbook (local only)
DEPLOYMENT.md

# Local-only review notes and draft docs. Folder-level rule so anything
# dropped in here stays local by default.
audit/

# Logs
*.log

# Coverage
.coverage
.coverage.*
htmlcov/

# Node (Vitest + Playwright)
node_modules/
coverage/
playwright-report/
test-results/
/playwright/.cache/

# Claude Code local state (scheduled-task lock files, etc.). Per-checkout,
# per-session; nothing in here should ever be committed.
.claude/
