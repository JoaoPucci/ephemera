# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environment
venv/
.venv/
env/

# Local secrets & data
.env
*.db
*.db-journal
*.db-wal
*.db-shm

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Private runbook (local only)
DEPLOYMENT.md

# Local-only review notes and draft docs. Folder-level rule so anything
# dropped in here stays local by default.
audit/

# Logs
*.log

# Coverage
.coverage
.coverage.*
coverage.xml
coverage.json
htmlcov/

# Mutmut. `mutants/` is the sandbox mutmut copies the codebase into for
# mutation runs; `mutmut-stats.json` is its progress cache. Both are
# generated artifacts of the weekly mutation-test workflow (and any
# local smoke runs); never committed.
mutants/
mutmut-stats.json

# Node (Vitest + Playwright)
node_modules/
coverage/
playwright-report/
test-results/
/playwright/.cache/

# Claude Code state. Mixed: some subdirs are shareable team config that
# encodes how Claude works on this codebase (agents, skills, commands)
# and should travel with the repo; the rest is per-checkout / per-session
# (memory caches, lockfiles, local settings) and must never be committed.
# Default-deny: explicit allowlist for the shareable subdirs only, so
# any new private path Claude Code adds in the future stays blocked
# unless we opt into it.
.claude/*
!.claude/agents/
!.claude/skills/
!.claude/commands/
