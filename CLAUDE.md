# CLAUDE.md

The operating contract for this repo is `AGENTS.md` — the same file
every other coding agent in this project reads. Claude Code does not
load `AGENTS.md` natively, so it is imported below; Claude-specific
notes, if any are ever needed, go *after* the import.

@AGENTS.md
