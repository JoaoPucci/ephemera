# Dev-time extras: everything you need for running the test suite.
# `-r requirements.txt` pulls in the runtime deps so one install covers both.
-r requirements.txt

pytest>=8.0
pytest-cov>=6.0
httpx>=0.27
